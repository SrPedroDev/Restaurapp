<form method="post" action="{{ path('app_pedido_item_delete', {'id': pedido_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pedido_item.id) }}">
    <button class="btn">Delete</button>
</form>
